DATA_SOURCE_NAME="{{ postgres_exporter_data_source_name }}"
OPTIONS="--log.level={{ postgres_exporter_log_level }} \
         --log.format={{ postgres_exporter_log_format }} \
{% for opt in postgres_exporter_options %}
         {{ opt }} \
{% endfor %}
         --web.listen-address={{ postgres_exporter_web_listen_address }} \
         --web.telemetry-path={{ postgres_exporter_web_telemetry_path }}"
